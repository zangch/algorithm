单调栈

栈内元素单调，插入元素时维护单调性。根据出栈区分单调性（与栈顶到栈底的单调性一致）。

保证局部的单调性
如：
单调递增栈，元素从左向右入栈，某一元素从左向右的所有小于该元素值的元素均会出现在该元素出栈前入栈。即在元素出栈时其左侧 （能观察到的值、直接大于的值） 均会在栈中。
单调递减栈，出栈时改变元素下标，扩展元素左右单调范围（向左向右单调性一致）。
所求的区间内（向左 || 向右）满足单调性。
